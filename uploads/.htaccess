# Security for uploads directory
# Allow images but prevent PHP execution

# Deny access to PHP files
<FilesMatch "\.php$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Disable PHP execution
php_flag engine off
